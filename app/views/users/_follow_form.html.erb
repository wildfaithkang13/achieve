<!--
<div id="follow_form_<%= user.id %>">
  <% unless current_user.following?(user) %>
  <p class="card-text">If you following, push follow button!</p>
    <%= form_for(current_user.relationships.build(followed_id: user.id), remote: true) do |f| %>
      <%= f.hidden_field :followed_id %>
      <%= f.submit "フォロー", class: "btn btn-large btn-primary" %>
    <% end %>
  <% else %>
  <p class="card-text"> Thank you following me!</p>
    <%= form_for(current_user.relationships.find_by(followed_id: user.id), html: { method: :delete }, remote: true) do |f| %>
      <%= f.submit "つながりを解除", class: "btn btn-large" %>
    <% end %>
  <% end %>
</div>
-->
<div class="card">
  <div id="follow_form_<%= user.id %>">
    <% unless current_user.following?(user) %>
      <p>If you following, push follow button!</p>
        <%= form_for(current_user.relationships.build(followed_id: user.id), remote: true) do |f| %>
          <%= f.hidden_field :followed_id %>
          <%= f.submit "フォロー", class: "btn btn-large btn-primary" %>
        <% end %>
    </div><br>
    <% else %>
      <p class="card-text"> Thank you following me!</p>
        <%= form_for(current_user.relationships.find_by(followed_id: user.id), html: { method: :delete }, remote: true) do |f| %>
          <%= f.submit "つながりを解除", class: "btn btn-large" %>
        <% end %>
    </div><br>
    <% end %>
  </div>
</div>
